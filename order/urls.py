# from django.conf.urls import patterns, include, url
# from order import views as order_views
#
# urlpatterns = patterns('',
#     url(r'^checkout/$', order_views.Checkout.as_view(), name='order_checkout'),
# )
